# ONE
http.get./postgresql/user/{id}:
  summary: Fetch User
  description: Fetch User from database
  fn: com.biz.postgresql.user.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    content:
      application/json:
        schema:
          type: object

# CREATE
http.post./postgresql/user:
  summary: Create a new User
  description: Create User from database
  fn: com.biz.postgresql.user.create
  body:
    content:
      application/json:
        schema:
          $ref: '#/definitions/postgresql/User'
  responses:
    content:
      application/json:
        schema:
          type: object

# UPDATE
http.put./postgresql/user/{id}:
  summary: Update a User
  description: Update User from database
  fn: com.biz.postgresql.user.update
  body:
    content:
      application/json:
        schema:
          $ref: '#/definitions/postgresql/User'
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    content:
      application/json:
        schema:
          type: object

# DELETE
http.delete./postgresql/user/{id}:
  summary: Delete a User
  description: Delete User from database
  fn: com.biz.postgresql.user.delete
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    content:
      application/json:
        schema:
          type: object

# SEARCH
http.post./postgresql/user/search:
  summary: Fetch multiple User
  description: Fetch multiple User from database
  fn: com.biz.postgresql.user.search
  body:
    content:
      application/json:
        schema:
          type: object
  responses:
    content:
      application/json:
        schema:
          type: array

